{
  "name": "workers-node-compat-report",
  "private": "true",
  "devDependencies": {
    "prettier": "^3.2.5",
    "shelljs": "^0.8.5"
  },
  "scripts": {
    "generate:script": "node ./generate-worker-script.mjs > worker.mjs && prettier worker.mjs --write && pnpm run copy:script",
    "copy:script": "cp worker.mjs wrangler-v3-polyfills && cp worker.mjs wrangler-jspm-polyfills && cp worker.mjs wrangler-unenv-polyfills && rm worker.mjs",
    "generate:apis": "mkdir -p generated && node ./generate-report.mjs && cp -r generated report2/src/ && rm -rf generated",
    "generate:report": "mkdir -p dist && node ./report/index.mjs && open dist/index.html",
    "generate": "pnpm generate:apis && pnpm generate:report"
  }
}
